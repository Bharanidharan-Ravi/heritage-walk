{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Debug": {
    "_comment": "DetailedErrors=true returns full 401/403/500 diagnostics in responses. Dev only — enable via `dotnet user-secrets set Debug:DetailedErrors true`, never in production.",
    "DetailedErrors": false
  },
  "EmailSettings": {
    "SmtpServer": "smtp.zoho.in",
    "SmtpPort": 587,
    "SenderEmail": "infos@archaeotrails.com",
    "SenderName": "Archaeo Trails",
    "AppPassword": "REPLACE_ME"
  },
  "PublicSiteUrl": "https://archaeotrails.com",
  "ConnectionStrings": {
    "_comment": "TODO(form-generator): set the real Azure SQL / local SQL Server connection strings via `dotnet user-secrets` (dev) or Azure App Service Configuration (prod) — never here.",
    "AzureSql": "REPLACE_ME",
    "SqlServerConnection": "REPLACE_ME"
  },
  "Razorpay": {
    "_comment": "TODO(form-generator): set via user-secrets / Azure App Service Configuration — never here. KeySecret must stay server-side only.",
    "KeyId": "REPLACE_ME",
    "KeySecret": "REPLACE_ME"
  },
  "Jwt": {
    "_comment": "TODO(admin-panel): real values must come from User Secrets in Development or Azure App Service Configuration in Production. Never store real secrets here.",
    "Issuer": "ArchaeoTrailsApi",
    "Audience": "ArchaeoTrailsAdminPanel",
    "Key": "REPLACE_ME",
    "ExpiryMinutes": 60
  },
  "Sanity": {
    "_comment": "TODO(experiences): set Sanity:WriteToken via user-secrets / Azure App Service Configuration — never here, and never expose it to the frontend. ProjectId/Dataset match UI/src/sanityClient.js.",
    "ProjectId": "nh8jhz7r",
    "Dataset": "production",
    "WriteToken": "REPLACE_ME"
  },
  "SeedAdmin": {
    "_comment": "TODO(admin-panel): real values must come from User Secrets in Development or Azure App Service Configuration in Production. Never store real secrets here. Left as REPLACE_ME, no initial Admin account is seeded — see IdentitySeeder.cs. UserName is the login handle for that first Admin (separate from Email); it is not a secret, and falls back to \"admin\" if unset.",
    "UserName": "admin",
    "Email": "REPLACE_ME",
    "Password": "REPLACE_ME"
  }
}
