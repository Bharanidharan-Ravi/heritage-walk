{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "EmailSettings": {
    "SmtpServer": "smtp.zoho.in",
    "SmtpPort": 587,
    "SenderEmail": "infos@archaeotrails.com",
    "SenderName": "Archaeo Trails",
    "AppPassword": "REPLACE_ME"
  },
  "PublicSiteUrl": "https://archaeotrails.com",
  "ConnectionStrings": {
    "_comment": "TODO(form-generator): set the real Azure SQL connection string via `dotnet user-secrets` (dev) or Azure App Service Configuration (prod) — never here.",
    "AzureSql": "Server=tcp:ak-trailsne.database.windows.net,1433;Initial Catalog=aktrailsnew;Authentication=Active Directory Default;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
  },
  "Razorpay": {
    "_comment": "TODO(form-generator): set via user-secrets / Azure App Service Configuration — never here. KeySecret must stay server-side only.",
    "KeyId": "REPLACE_ME",
    "KeySecret": "REPLACE_ME"
  },
  "Jwt": {
    "_comment": "TODO(admin-panel): real values must come from User Secrets in Development or Azure App Service Configuration in Production. Never store real secrets here.",
    "Issuer": "ArchaeoTrailsApi",
    "Audience": "ArchaeoTrailsAdminPanel",
    "Key": "REPLACE_ME",
    "ExpiryMinutes": 60
  },
  "SeedAdmin": {
    "_comment": "TODO(admin-panel): real values must come from User Secrets in Development or Azure App Service Configuration in Production. Never store real secrets here. Left as REPLACE_ME, no initial Admin account is seeded — see IdentitySeeder.cs.",
    "Email": "REPLACE_ME",
    "Password": "REPLACE_ME"
  }
}
